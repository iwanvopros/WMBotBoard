name: Tägliche WM-Tipps

# FALLBACK-Lauf: 07:35 UTC = 09:35 Europe/Zurich (CEST, gilt während der WM Jun–Jul). Die
# primäre Claude-Routine läuft um 09:05 und reichert die Tipps an; dieser Job springt nur
# ein, wenn sie ausgefallen ist (predict.py überschreibt angereicherte Tipps nicht). Gratis.
on:
  schedule:
    - cron: "35 7 * * *"
  workflow_dispatch:
    inputs:
      date:
        description: "Datum YYYY-MM-DD (leer = heute, Europe/Zurich)"
        required: false
        default: ""

permissions:
  contents: write

concurrency:
  group: daily-tips
  cancel-in-progress: false

jobs:
  predict:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python einrichten
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Tipps berechnen & veröffentlichen
        run: bash scripts/run_daily.sh "${{ github.event.inputs.date }}"

      - name: Änderungen committen
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "Keine Änderungen."
          else
            git commit -m "Tipps $(TZ=Europe/Zurich date +%F) [auto]"
            git push
          fi
